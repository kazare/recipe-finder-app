import React from 'react';

const Search = () => {
    return (
        <div>
            <form>
                <input type="text" />
                <button>Search</button>
            </form>
        </div>
    );
}

export default Search;